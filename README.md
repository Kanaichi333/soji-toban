# 掃除当番

1. 今週の掃除当番を `soji_toban_input.txt` に入力して保存する
2. `soji_toban.py` を実行
3. 来週の掃除当番が出力される


### Macの場合のみ

ターミナルで以下を実行
```
cd soji_toban
```

```
chmod +x soji_toban
```

Unix実行ファイル `soji_toban` をダブルクリックすることで `soji_toban.py` を実行可能
